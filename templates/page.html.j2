<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            background-color: #d8d8d8;
            text-align: center;
        }

        nav {
            font-size: 36px;
            font-weight: bold;
            margin-top: 50px;
            margin-bottom: 30px;
        }

        h1 {
            font-size: 40px;
            margin-top: 0;
        }

        h2 {
            font-size: 30px;
            margin-top: 0;
        }

        h3 {
            font-size: 24px;
            margin-top: 0;
        }

        h4 {
            font-size: 20px
            margin-top: 0;
        }

        h5, h6 {
            font-size: 18px
            margin-top: 0;
        }

        p {
            font-size: 18px;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        main {
            width: 50%;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            border-radius: 20px;
            text-align: left;
        }

        footer {
            display: flex;
            flex-direction: row;
            width: auto;
            padding-top: 2px;
            margin: 15px;
            border-top: 1px solid lightgrey;
            font-size: 14px;
        }

        @media (max-width: 767px) {
            main {
                width: 90%;
                margin: 30px auto;
            }
        }
    </style>
</head>
<body>
    <nav>
        <b>
            <a style="color: inherit; text-decoration: inherit;" href="https://{{ domain }}">{{ domain }}</a>
        </b>
    </nav>
    <main>
        <h1 style="text-align: center;">{{ title }}</h1>
        <div id="content">{{ content }}</div>
    </main>
    <footer>
        <span style="margin-right: auto;">generated with <3 by wikie v{{ version }}</span>
        <span>Last updated: {{ last_updated }}</span>
    </footer>
</body>
</html>
